// Release switches. The NFA / PMS / vendors suite is fully built but hidden
// for this release — flip to true to expose the admin sections and the /ess
// employee portal again.
export const FEATURES = {
  nfaSuite: true,
};
